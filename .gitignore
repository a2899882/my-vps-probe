# Runtime configuration and state
config.json
data.db
data.db-shm
data.db-wal
usage_state.json
server.log

# Locally built executables
probe-server
probe-agent
probe-agent-amd64
probe-agent-arm64
server/probe-agent-amd64
server/probe-agent-arm64
server/.probe-agent-*.new
.probe-server.new

# Backups and temporary migration files
backups/
*.tar.gz
*.bak
*.bak.*
*.before-*
*.failed.*
*.broken.*
*.compact.bak.*
*.dark-loss.bak.*
*.light-ui.bak.*
.restore-*/

# Frontend test dependencies (not needed on a VPS)
node_modules/
